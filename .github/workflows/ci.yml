name: Full Project CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18
        cache: 'npm'
        cache-dependency-path: frontend/book-website/package-lock.json
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.10
    
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r backend/api/requirements.txt
    
    - name: Install Node dependencies
      working-directory: frontend/book-website
      run: npm ci
    
    - name: Check for file formatting issues
      run: |
        # Add any formatting checks here
        echo "Checking code formatting..."
    
    - name: Security audit
      run: |
        # Add security scanning tools here
        echo "Running security audit..."

  docker-build:
    name: Docker Build Test
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Build Docker images
      run: |
        echo "Skipping Docker build in CI for now to avoid resource issues"